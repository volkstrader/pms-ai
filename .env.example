# pms-ai — example environment. Copy to `.env` and fill in real values.
# NEVER commit `.env` (it is git-ignored). Or skip the file entirely and inject
# secrets at runtime with `varlock run -- pms-ai ...` / `infisical run -- pms-ai ...`.

PMS_AI_TOKEN=replace-me
